version: '2'
services:
  db:
    image: postgres
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=root
      - POSTGRES_DB=sensui
      - POSTGRES_PASSWORD=sensui
    volumes:
    - ./sql:/docker-entrypoint-initdb.d